if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks не установлен (brew install gitleaks) — коммит остановлен" >&2
  exit 1
fi
gitleaks git --staged --config .gitleaks.toml --no-banner --redact

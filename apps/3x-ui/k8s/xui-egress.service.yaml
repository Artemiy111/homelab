apiVersion: v1
kind: Service
metadata:
  name: xui-egress
  namespace: 3x-ui
spec:
  # HTTP forward proxy (порт 8440) на IP узла: Gatus ходит на
  # http://192.0.2.10:8440 (см. client.proxy-url для Telegram).
  type: ClusterIP
  externalIPs:
    - 192.0.2.10
  selector:
    app: 3x-ui
  ports:
    - name: egress
      port: 8440
      targetPort: egress
      protocol: TCP

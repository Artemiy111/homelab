apiVersion: v1
kind: Service
metadata:
  name: athens
  namespace: athens
spec:
  type: ClusterIP
  selector:
    app: athens
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

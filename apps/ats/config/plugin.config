# plugin.config — плагины, загружаемые при старте.
# stats_over_http: отдаёт статистику процесса по HTTP (штатного /metrics у ATS
# нет). PURGE-плагин сюда не пишется: remap_purge — это remap-плагин, он
# подключается в remap.config через @plugin= (см. config/remap.config).
stats_over_http.so

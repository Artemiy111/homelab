# plugin.config — плагины, загружаемые при старте.
# remap_purge: обработка PURGE-запросов (ручная инвалидация объекта по URL).
# stats_over_http: отдаёт статистику процесса по HTTP (используется внешним
# Prometheus-exporter'ом, штатного /metrics у ATS нет).
remap_purge.so
stats_over_http.so

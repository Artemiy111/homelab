# storage.config — где и сколько диска под кэш.
# Путь монтируется из PVC (volumeMounts.cache). Размер должен совпадать с
# requests.storage PVC: ATS резервирует файл кэша фиксированного размера.
# Cache.db создаётся самим ATS при старте.
/var/cache/trafficserver 20G

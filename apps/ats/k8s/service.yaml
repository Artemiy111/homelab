apiVersion: v1
kind: Service
metadata:
  name: ats
  namespace: ats
spec:
  type: ClusterIP
  selector:
    app: ats
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

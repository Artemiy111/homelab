apiVersion: v1
kind: Service
metadata:
  name: authentik-postgresql
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: authentik-postgresql
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP

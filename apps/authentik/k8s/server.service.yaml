apiVersion: v1
kind: Service
metadata:
  name: authentik
  namespace: authentik
spec:
  type: ClusterIP
  selector:
    app: authentik
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
    # Prometheus-метрики (скрейпит vmagent).
    - name: metrics
      protocol: TCP
      port: 9300
      targetPort: 9300

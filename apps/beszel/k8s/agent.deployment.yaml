apiVersion: apps/v1
kind: Deployment
metadata:
  name: beszel-agent
  namespace: beszel
  annotations:
    ignore-check.kube-linter.io/docker-sock: "агент читает docker.sock read-only для метрик контейнеров (#161)"
    ignore-check.kube-linter.io/host-network: "агент слушает unix-сокет, общий с hub, и собирает метрики хоста — нужен hostNetwork (#161)"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: beszel-agent
  template:
    metadata:
      labels:
        app: beszel-agent
    spec:
      dnsPolicy: ClusterFirstWithHostNet
      # Агент собирает метрики хоста и Docker-контейнеров и слушает unix-сокет,
      # общий с hub: hostNetwork + /var/run/docker.sock.
      hostNetwork: true
      enableServiceLinks: false
      containers:
        - name: beszel-agent
          image: "henrygd/beszel-agent:0.18.7@sha256:8874e2c53f9de5e063a6a80d6b617e20fa593ac5dc4eb4c6ce1f912f510f38f8"
          env:
            - name: DOMAIN
              valueFrom:
                configMapKeyRef:
                  name: homelab-config
                  key: DOMAIN
            - name: TZ
              valueFrom:
                configMapKeyRef:
                  name: homelab-config
                  key: TZ
            - name: BESZEL_AGENT_KEY
              valueFrom:
                secretKeyRef:
                  name: beszel
                  key: BESZEL_AGENT_KEY
            - name: BESZEL_AGENT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: beszel
                  key: BESZEL_AGENT_TOKEN
            - name: LISTEN
              value: /beszel_socket/beszel.sock
            - name: HUB_URL
              value: "https://beszel.$(DOMAIN)"
            - name: KEY
              value: "$(BESZEL_AGENT_KEY)"
            - name: TOKEN
              value: "$(BESZEL_AGENT_TOKEN)"
          resources:
            limits:
              cpu: "1"
              memory: 512Mi
            requests:
              cpu: 50m
              memory: 64Mi
          volumeMounts:
            - name: agent
              mountPath: /var/lib/beszel-agent
            - name: socket
              mountPath: /beszel_socket
            - name: docker-sock
              readOnly: true
              mountPath: /var/run/docker.sock
          livenessProbe:
            exec:
              command: ["/agent", "health"]
            timeoutSeconds: 5
            periodSeconds: 120
            failureThreshold: 3
          readinessProbe:
            exec:
              command: ["/agent", "health"]
            timeoutSeconds: 5
            periodSeconds: 120
            failureThreshold: 3
          startupProbe:
            exec:
              command: ["/agent", "health"]
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 30
          securityContext:
            capabilities:
              drop: ["ALL"]
            allowPrivilegeEscalation: false
            runAsUser: 0
            runAsGroup: 0
            seccompProfile:
              type: RuntimeDefault
      volumes:
        - name: agent
          persistentVolumeClaim:
            claimName: beszel-agent
        - name: socket
          hostPath:
            path: /storage/apps/beszel/socket
            type: Directory
        - name: docker-sock
          hostPath:
            path: /var/run/docker.sock
            type: Socket
  strategy:
    type: Recreate
  revisionHistoryLimit: 2

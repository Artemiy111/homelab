apiVersion: v1
kind: Service
metadata:
  name: beszel
  namespace: beszel
spec:
  type: ClusterIP
  selector:
    app: beszel
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

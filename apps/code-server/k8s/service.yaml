apiVersion: v1
kind: Service
metadata:
  name: code-server
  namespace: code-server
spec:
  type: ClusterIP
  selector:
    app: code-server
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

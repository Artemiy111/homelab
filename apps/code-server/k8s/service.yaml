apiVersion: v1
kind: Service
metadata:
  name: code-server
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: code-server
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: dawarich
  namespace: dawarich
spec:
  type: ClusterIP
  selector:
    app: dawarich
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

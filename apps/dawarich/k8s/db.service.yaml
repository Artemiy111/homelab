apiVersion: v1
kind: Service
metadata:
  name: dawarich-db-legacy
  namespace: dawarich
spec:
  type: ClusterIP
  selector:
    app: dawarich-db-legacy
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: dawarich-db
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: dawarich-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP

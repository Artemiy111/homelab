apiVersion: v1
kind: Service
metadata:
  name: dawarich-redis
  namespace: dawarich
spec:
  type: ClusterIP
  selector:
    app: dawarich-redis
  ports:
    - name: redis
      protocol: TCP
      port: 6379
      targetPort: redis

apiVersion: v1
kind: Service
metadata:
  name: dawarich-redis
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: dawarich-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
      protocol: TCP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: seafile-mariadb-exporter
  namespace: seafile
spec:
  replicas: 1
  selector:
    matchLabels:
      app: seafile-mariadb-exporter
  revisionHistoryLimit: 2
  template:
    metadata:
      labels:
        app: seafile-mariadb-exporter
    spec:
      enableServiceLinks: false
      containers:
        - name: mysqld-exporter
          image: "quay.io/prometheus/mysqld-exporter:v0.20.0@sha256:abed8dac117b4ae5b70757f988e44795935b9a72c3b58d720c67ba688f8cb79e"
          args:
            - --mysqld.address=seafile-mariadb.seafile.svc.cluster.local:3306
            - --mysqld.username=root
          ports:
            - name: metrics
              containerPort: 9104
          env:
            - name: MYSQLD_EXPORTER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: seafile
                  key: INIT_SEAFILE_MYSQL_ROOT_PASSWORD
          resources:
            limits:
              cpu: 100m
              memory: 64Mi
            requests:
              cpu: 10m
              memory: 32Mi
          livenessProbe:
            httpGet:
              path: /metrics
              port: metrics
            timeoutSeconds: 5
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /metrics
              port: metrics
            timeoutSeconds: 5
            periodSeconds: 30
            failureThreshold: 3
          securityContext:
            capabilities:
              drop: ["ALL"]
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 65534
            runAsGroup: 65534
            seccompProfile:
              type: RuntimeDefault
---
apiVersion: v1
kind: Service
metadata:
  name: seafile-mariadb-exporter
  namespace: seafile
spec:
  type: ClusterIP
  selector:
    app: seafile-mariadb-exporter
  ports:
    - name: metrics
      protocol: TCP
      port: 9104
      targetPort: metrics

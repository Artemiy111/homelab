apiVersion: v1
kind: Service
metadata:
  name: element-web
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: element-web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP

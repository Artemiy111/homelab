apiVersion: v1
kind: Service
metadata:
  name: livekit
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: element-livekit
  ports:
    - name: http
      port: 7880
      targetPort: http
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: matrix-rtc-auth
  namespace: element
spec:
  type: ClusterIP
  selector:
    app: matrix-rtc-auth
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

apiVersion: v1
kind: Service
metadata:
  name: matrix-rtc-auth
  namespace: element
spec:
  type: ClusterIP
  selector:
    app: matrix-rtc-auth
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

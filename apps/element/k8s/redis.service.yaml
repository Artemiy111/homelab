apiVersion: v1
kind: Service
metadata:
  name: element-redis
  namespace: element
spec:
  type: ClusterIP
  selector:
    app: element-redis
  ports:
    - name: redis
      protocol: TCP
      port: 6379
      targetPort: redis

apiVersion: v1
kind: Service
metadata:
  name: synapse
  namespace: element
spec:
  type: ClusterIP
  selector:
    app: element-synapse
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
    # Prometheus-метрики (скрейпит vmagent).
    - name: metrics
      protocol: TCP
      port: 9009
      targetPort: metrics

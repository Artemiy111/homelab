apiVersion: v1
kind: Service
metadata:
  name: gatus
  namespace: monitoring
spec:
  type: ClusterIP
  selector:
    app: gatus
  ports:
    - name: web
      port: 8080
      targetPort: 8080
      protocol: TCP
apiVersion: v1
kind: Service
metadata:
  name: gatus
  namespace: monitoring
spec:
  type: ClusterIP
  selector:
    app: gatus
  ports:
    - name: web
      protocol: TCP
      port: 80
      targetPort: 8080

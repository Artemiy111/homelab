apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
resources:
  - k8s/deployment.yaml
  - k8s/sealedsecret.yaml
  - k8s/service.yaml
configMapGenerator:
  - name: gatus-config
    files:
      - config/config.yaml

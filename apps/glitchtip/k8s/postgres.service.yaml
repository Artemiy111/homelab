apiVersion: v1
kind: Service
metadata:
  name: glitchtip-postgres
  namespace: glitchtip
spec:
  type: ClusterIP
  selector:
    app: glitchtip-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
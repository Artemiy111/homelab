apiVersion: v1
kind: Service
metadata:
  name: glitchtip
  namespace: glitchtip
spec:
  type: ClusterIP
  selector:
    app: glitchtip
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
apiVersion: apps/v1
kind: Deployment
metadata:
  name: home-assistant
  namespace: home-assistant
  annotations:
    ignore-check.kube-linter.io/host-network: "mDNS/SSDP/DHCP discovery устройств требует hostNetwork (#161)"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: home-assistant
  template:
    metadata:
      labels:
        app: home-assistant
    spec:
      dnsPolicy: ClusterFirstWithHostNet
      # hostNetwork нужен для локального обнаружения устройств (mDNS, SSDP, DHCP).
      hostNetwork: true
      enableServiceLinks: false
      containers:
        - name: home-assistant
          image: "ghcr.io/home-assistant/home-assistant:stable@sha256:56690a89c79a0de98035e1719f8324a92d5859c1192ff45adb0230ea81cb42a5"
          ports:
            - name: http
              containerPort: 8123
          env:
            - name: TZ
              valueFrom:
                configMapKeyRef:
                  name: homelab-config
                  key: TZ
            - name: OIDC_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: home-assistant
                  key: OIDC_CLIENT_ID
            - name: OIDC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: home-assistant
                  key: OIDC_CLIENT_SECRET
          resources:
            limits:
              cpu: "2"
              memory: 2Gi
            requests:
              cpu: 200m
              memory: 512Mi
          volumeMounts:
            - name: config
              mountPath: /config
            - name: configuration
              readOnly: true
              mountPath: /config/configuration.yaml
              subPath: configuration.yaml
            - name: localtime
              readOnly: true
              mountPath: /etc/localtime
          livenessProbe:
            httpGet:
              path: /
              port: http
            timeoutSeconds: 10
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: http
            timeoutSeconds: 10
            periodSeconds: 30
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 30
          securityContext:
            capabilities:
              # NET_RAW — пассивный DHCP discovery/ICMP; DAC_OVERRIDE — запись
              # в artlab-owned маунты /config без подгона владельцев под root.
              add: ["NET_RAW", "DAC_OVERRIDE"]
              drop: ["ALL"]
            # no-new-privileges из compose = allowPrivilegeEscalation: false.
            allowPrivilegeEscalation: false
            runAsUser: 0
            runAsGroup: 0
            seccompProfile:
              type: RuntimeDefault
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: home-assistant-config
        - name: configuration
          configMap:
            name: home-assistant-configuration
        - name: localtime
          hostPath:
            path: /etc/localtime
            type: File
  # RWO-том конфига: два пода при rolling update пишут в один каталог.
  strategy:
    type: Recreate
  revisionHistoryLimit: 2

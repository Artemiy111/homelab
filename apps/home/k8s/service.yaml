apiVersion: v1
kind: Service
metadata:
  name: home
  namespace: home
spec:
  type: ClusterIP
  selector:
    app: home
  ports:
    - name: home-web
      port: 3000
      targetPort: 3000
      protocol: TCP
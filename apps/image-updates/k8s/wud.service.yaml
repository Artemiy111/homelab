apiVersion: v1
kind: Service
metadata:
  name: wud
  namespace: monitoring
spec:
  type: ClusterIP
  selector:
    app: wud
  ports:
    # gatus ходит на http://wud/health (порт 80).
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
    # vmagent скрейпит http://wud:3000/metrics.
    - name: metrics
      protocol: TCP
      port: 3000
      targetPort: http

apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: wud
  namespace: monitoring
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`wud.example.com`)
      kind: Rule
      middlewares:
        - name: oauth2-proxy
          namespace: default
        - name: secure-headers
          namespace: default
        - name: ratelimit-default
          namespace: default
      services:
        - name: wud
          port: 80

apiVersion: v1
kind: Service
metadata:
  name: immich-db
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: immich-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP

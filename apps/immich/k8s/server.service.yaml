apiVersion: v1
kind: Service
metadata:
  name: immich
  namespace: immich
spec:
  type: ClusterIP
  selector:
    app: immich-server
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
    # Prometheus-метрики (скрейпит vmagent).
    - name: metrics
      protocol: TCP
      port: 8081
      targetPort: 8081

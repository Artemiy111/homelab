apiVersion: v1
kind: Service
metadata:
  name: immich
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: immich-server
  ports:
    - name: http
      port: 2283
      targetPort: http
      protocol: TCP

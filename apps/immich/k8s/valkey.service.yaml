apiVersion: v1
kind: Service
metadata:
  name: immich-valkey
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: immich-valkey
  ports:
    - name: redis
      port: 6379
      targetPort: redis
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: infisical-postgres
  namespace: infisical
spec:
  type: ClusterIP
  selector:
    app: infisical-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP

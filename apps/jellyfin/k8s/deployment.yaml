apiVersion: apps/v1
kind: Deployment
metadata:
  name: jellyfin
  namespace: default
spec:
  # hostPath-каталоги нельзя монтировать в два пода одновременно.
  strategy:
    type: Recreate
  revisionHistoryLimit: 2
  replicas: 1
  selector:
    matchLabels:
      app: jellyfin
  template:
    metadata:
      labels:
        app: jellyfin
    spec:
      # Jellyfin читает переменные с префиксом JELLYFIN_ как конфиг-override,
      # а service-links инжектит JELLYFIN_PORT=tcp://… — ломает порт сервера.
      enableServiceLinks: false
      # Доступ к /dev/dri/renderD128 (группа render на хосте).
      securityContext:
        supplementalGroups: [105]
      containers:
        - name: jellyfin
          image: "jellyfin/jellyfin:10@sha256:aefb67e6a7ff1debdd154a78a7bbb780fd0c873d8639210a7f6a2016ad2b35db"
          ports:
            - name: http
              containerPort: 8096
          env:
            - name: DOMAIN
              valueFrom:
                configMapKeyRef:
                  name: homelab-config
                  key: DOMAIN
            - name: TZ
              valueFrom:
                configMapKeyRef:
                  name: homelab-config
                  key: TZ
            - name: JELLYFIN_PublishedServerUrl
              value: "https://jellyfin.$(DOMAIN)"
          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 5
            failureThreshold: 60
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "2"
              memory: 2Gi
          volumeMounts:
            - name: config
              mountPath: /config
            - name: cache
              mountPath: /cache
            - name: media
              mountPath: /media
              readOnly: true
            - name: dri
              mountPath: /dev/dri
      volumes:
        - name: config
          hostPath:
            path: /storage/apps/jellyfin/config
            type: Directory
        - name: cache
          hostPath:
            path: /storage/apps/jellyfin/cache
            type: Directory
        - name: media
          hostPath:
            path: /storage/media
            type: Directory
        - name: dri
          hostPath:
            path: /dev/dri
            type: Directory

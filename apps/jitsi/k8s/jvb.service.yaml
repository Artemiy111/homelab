apiVersion: v1
kind: Service
metadata:
  name: jitsi-jvb
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: jitsi-jvb
  ports:
    - name: colibri
      port: 8080
      targetPort: colibri
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: local-ai
  namespace: local-ai
spec:
  type: ClusterIP
  selector:
    app: local-ai
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

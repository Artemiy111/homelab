apiVersion: v1
kind: Service
metadata:
  name: local-ai
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: local-ai
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

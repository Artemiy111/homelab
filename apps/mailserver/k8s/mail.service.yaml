apiVersion: v1
kind: Service
metadata:
  name: mail
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: mail
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP

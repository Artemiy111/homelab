apiVersion: v1
kind: Service
metadata:
  name: mailserver
  namespace: mailserver
spec:
  type: ClusterIP
  selector:
    app: mailserver
  ports:
    # JMAP/API/админка Stalwart за Traefik (mailserver.<домен>).
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

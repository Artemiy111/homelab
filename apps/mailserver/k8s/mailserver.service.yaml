apiVersion: v1
kind: Service
metadata:
  name: mailserver
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: mailserver
  ports:
    # JMAP/API/админка Stalwart за Traefik (mailserver.<домен>).
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

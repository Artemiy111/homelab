# Мост k8s → Docker: Gatus ходит по имени `netdata`, трафик уходит на
# docker-контейнер через порт, опубликованный на шлюзе traefiknet
# (см. `ports` в apps/netdata/compose.yaml). При миграции сервиса в k8s
# заменить EndpointSlice на selector — имя и порт для Gatus не меняются.
apiVersion: v1
kind: Service
metadata:
  name: netdata
  namespace: default
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: netdata-bridge
  namespace: default
  labels:
    kubernetes.io/service-name: netdata
addressType: IPv4
ports:
  - name: http
    port: 18081
    protocol: TCP
endpoints:
  - addresses:
      - 172.20.0.1
    conditions:
      ready: true

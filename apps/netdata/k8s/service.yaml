apiVersion: v1
kind: Service
metadata:
  name: netdata
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: netdata
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
    # Порт приложения: vmagent скрейпит /api/v1/allmetrics.
    - name: scrape
      port: 19999
      targetPort: http
      protocol: TCP

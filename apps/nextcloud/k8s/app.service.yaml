apiVersion: v1
kind: Service
metadata:
  name: nextcloud
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: nextcloud
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP

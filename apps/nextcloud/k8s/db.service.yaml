apiVersion: v1
kind: Service
metadata:
  # Имя = db: так записано dbhost в персистентном config.php Nextcloud.
  name: db
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: nextcloud-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: nextcloud-db
  namespace: nextcloud
spec:
  type: ClusterIP
  selector:
    app: nextcloud-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: nextcloud-exporter
  namespace: nextcloud
spec:
  type: ClusterIP
  selector:
    app: nextcloud-exporter
  ports:
    - name: metrics
      port: 9205
      targetPort: metrics
      protocol: TCP

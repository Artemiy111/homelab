apiVersion: v1
kind: Service
metadata:
  # Имя = redis: так записан host в config.php Nextcloud.
  name: redis
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: nextcloud-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
      protocol: TCP

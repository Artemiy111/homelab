apiVersion: v1
kind: Service
metadata:
  name: open-webui
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: open-webui
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: paperless-broker
  namespace: paperless
spec:
  type: ClusterIP
  selector:
    app: paperless-broker
  ports:
    - name: valkey
      protocol: TCP
      port: 6379
      targetPort: valkey

apiVersion: v1
kind: Service
metadata:
  name: paperless-broker
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: paperless-broker
  ports:
    - name: valkey
      port: 6379
      targetPort: valkey
      protocol: TCP

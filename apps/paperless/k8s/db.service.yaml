apiVersion: v1
kind: Service
metadata:
  name: paperless-db
  namespace: paperless
spec:
  type: ClusterIP
  selector:
    app: paperless-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP

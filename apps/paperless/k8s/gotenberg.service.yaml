apiVersion: v1
kind: Service
metadata:
  name: paperless-gotenberg
  namespace: paperless
spec:
  type: ClusterIP
  selector:
    app: paperless-gotenberg
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: http

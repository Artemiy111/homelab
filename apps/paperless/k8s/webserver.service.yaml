apiVersion: v1
kind: Service
metadata:
  name: paperless
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: paperless
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: paperless
  namespace: paperless
spec:
  type: ClusterIP
  selector:
    app: paperless
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

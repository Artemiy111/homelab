apiVersion: v1
kind: Service
metadata:
  name: stirlingpdf
spec:
  type: ClusterIP
  selector:
    app: stirlingpdf
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP

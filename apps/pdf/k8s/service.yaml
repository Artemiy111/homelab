apiVersion: v1
kind: Service
metadata:
  name: stirlingpdf
  namespace: pdf
spec:
  type: ClusterIP
  selector:
    app: stirlingpdf
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080

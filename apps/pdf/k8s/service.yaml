apiVersion: v1
kind: Service
metadata:
  name: stirlingpdf
  namespace: pdf
spec:
  type: ClusterIP
  selector:
    app: stirlingpdf
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: postgres-pgweb
  namespace: postgres
spec:
  type: ClusterIP
  selector:
    app: postgres-pgweb
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP

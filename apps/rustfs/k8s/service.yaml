apiVersion: v1
kind: Service
metadata:
  name: rustfs
  namespace: rustfs
spec:
  type: ClusterIP
  selector:
    app: rustfs
  ports:
    - name: s3
      protocol: TCP
      port: 80
      targetPort: s3
    - name: console
      protocol: TCP
      port: 9001
      targetPort: console

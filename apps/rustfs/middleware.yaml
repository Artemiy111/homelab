apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: rustfs-root-redirect
  namespace: rustfs
spec:
  redirectRegex:
    permanent: true
    regex: ^https://rustfs\.example\.com/$
    replacement: https://rustfs.example.com/rustfs/console/

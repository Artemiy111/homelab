apiVersion: v1
kind: Service
metadata:
  name: seafile-db
  namespace: seafile
spec:
  type: ClusterIP
  selector:
    app: seafile-db
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
      protocol: TCP

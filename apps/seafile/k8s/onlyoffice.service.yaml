apiVersion: v1
kind: Service
metadata:
  name: onlyoffice
  namespace: seafile
spec:
  type: ClusterIP
  selector:
    app: onlyoffice
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

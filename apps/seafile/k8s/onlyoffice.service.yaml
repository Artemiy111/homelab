apiVersion: v1
kind: Service
metadata:
  name: onlyoffice
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: onlyoffice
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP

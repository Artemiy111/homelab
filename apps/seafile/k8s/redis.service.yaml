apiVersion: v1
kind: Service
metadata:
  name: seafile-redis
  namespace: seafile
spec:
  type: ClusterIP
  selector:
    app: seafile-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
      protocol: TCP

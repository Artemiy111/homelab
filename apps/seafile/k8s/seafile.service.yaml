apiVersion: v1
kind: Service
metadata:
  name: seafile
  namespace: seafile
spec:
  type: ClusterIP
  selector:
    app: seafile
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

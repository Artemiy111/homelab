apiVersion: v1
kind: Service
metadata:
  name: sure-db
  namespace: sure
spec:
  type: ClusterIP
  selector:
    app: sure-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP

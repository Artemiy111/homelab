apiVersion: v1
kind: Service
metadata:
  name: sure
  namespace: sure
spec:
  type: ClusterIP
  selector:
    app: sure
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP

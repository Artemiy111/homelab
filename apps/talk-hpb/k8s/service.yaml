apiVersion: v1
kind: Service
metadata:
  name: talk-hpb
  namespace: talk-hpb
spec:
  type: ClusterIP
  selector:
    app: talk-hpb
  ports:
    - name: signaling
      port: 8081
      targetPort: signaling
      protocol: TCP

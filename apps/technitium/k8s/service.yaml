apiVersion: v1
kind: Service
metadata:
  name: technitium
  namespace: technitium
spec:
  type: ClusterIP
  selector:
    app: technitium
  ports:
    # Админка/API за Traefik (dns.<домен>).
    - name: http
      port: 5380
      targetPort: http
      protocol: TCP

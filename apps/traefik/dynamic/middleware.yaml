http:
  middlewares:
    ratelimit-default:
      rateLimit:
        average: 100
        burst: 50

    # Для SPA (Open WebUI): первая загрузка вываливает сотню статических
    # чанков одним махом и упирается в ratelimit-default, из-за чего
    # приложение падает уже на клиенте.
    ratelimit-spa:
      rateLimit:
        average: 400
        burst: 200

    oauth2-proxy:
      forwardAuth:
        # oauth2-proxy переехал в k8s: forwardAuth ходит на его публичный
        # host через ingress Traefik кластера (192.0.2.10), где запрос
        # принимает IngressRoute oauth2-proxy. Traefik дописывает исходный URI.
        address: https://oauth.example.com
        trustForwardHeader: true
        # Внутренний wildcard-сертификат не в системном CA контейнера.
        tls:
          insecureSkipVerify: true
        authResponseHeaders:
          - X-Auth-Request-User
          - X-Auth-Request-Email

    secure-headers:
      headers:
        stsSeconds: 31536000
        stsIncludeSubdomains: true
        stsPreload: false
        frameDeny: true
        contentTypeNosniff: true
    
    secure-headers-strict:
      headers:
        stsSeconds: 31536000
        stsIncludeSubdomains: true
        stsPreload: true
        frameDeny: true
        contentTypeNosniff: true
        referrerPolicy: "strict-origin-when-cross-origin"
        contentSecurityPolicy: >-
          default-src 'self';
          script-src 'self';
          style-src 'self' 'unsafe-inline';
          img-src 'self' data: https:;
          font-src 'self' data:;
          connect-src 'self';
          frame-ancestors 'none';
          object-src 'none';
          base-uri 'self'
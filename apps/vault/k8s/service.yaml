apiVersion: v1
kind: Service
metadata:
  name: vault
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: vault
  ports:
    - name: http
      port: 8200
      targetPort: http
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: verdaccio
  namespace: verdaccio
spec:
  type: ClusterIP
  selector:
    app: verdaccio
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

# Публичная конфигурация victoria-metrics stack.
# Секреты — в secrets.enc.env (SOPS).

# Хранение метрик в VictoriaMetrics single-node.
VM_RETENTION_PERIOD=180d

# Логин администратора Grafana; пароль в secrets.enc.env.
GRAFANA_ADMIN_USER=admin

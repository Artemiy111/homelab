apiVersion: v1
kind: Service
metadata:
  name: victoriametrics
  namespace: monitoring
spec:
  type: ClusterIP
  selector:
    app: victoriametrics
  ports:
    # Порт 80 сохранён для Gatus (http://victoriametrics/health).
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
    # Порт приложения: grafana datasource и vmagent remote write.
    - name: vm
      protocol: TCP
      port: 8428
      targetPort: http

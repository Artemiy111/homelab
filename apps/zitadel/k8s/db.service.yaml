apiVersion: v1
kind: Service
metadata:
  name: zitadel-postgres-legacy
  namespace: zitadel
spec:
  type: ClusterIP
  selector:
    app: zitadel-postgres-legacy
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP

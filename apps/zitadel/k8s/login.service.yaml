apiVersion: v1
kind: Service
metadata:
  name: zitadel-login
  namespace: zitadel
spec:
  type: ClusterIP
  selector:
    app: zitadel-login
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP

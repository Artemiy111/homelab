apiVersion: v1
kind: Service
metadata:
  name: zitadel
  namespace: zitadel
spec:
  type: ClusterIP
  selector:
    app: zitadel
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

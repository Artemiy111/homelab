apiVersion: v1
kind: Service
metadata:
  name: zot
  namespace: zot
spec:
  type: ClusterIP
  selector:
    app: zot
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

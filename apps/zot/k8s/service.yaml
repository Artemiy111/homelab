apiVersion: v1
kind: Service
metadata:
  name: zot
  namespace: zot
spec:
  type: ClusterIP
  selector:
    app: zot
  ports:
    - name: http
      port: 5000
      targetPort: http
      protocol: TCP

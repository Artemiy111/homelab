# Алиасы и функции для интерактивных bash-сессий.
# Основной шелл пользователя — zsh (см. .zshrc); этот файл — fallback.
# Неинтерактивные вызовы (bash -lc, scp/rsync) пропускаем через return,
# а не exit, чтобы не обрывать вызывающую сессию.
[[ $- == *i* ]] || return 0

# /etc/sudoers.d/ai-agent
# Позволяет ai-agent выполнять команды от имени artlab без пароля.
# Используется для эскалации: агент по умолчанию работает от ai-agent,
# при необходимости запрашивает разрешение и выполняет через sudo -u artlab.

ai-agent ALL=(artlab) NOPASSWD: ALL

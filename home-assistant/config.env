# Публичная конфигурация сервиса; подмешивается в окружение init.sh
# (см. service_init в scripts/lib/common.sh).

# Версия hass-oidc-auth (релиз с GitHub), которую устанавливает init.sh.
# Обновление: поменять версию здесь, закоммитить, запустить init.sh заново.
HOME_ASSISTANT_OIDC_VERSION=v1.2.1

# Общий домен homelab; хосты ниже образуются как <sub>.${DOMAIN}.
DOMAIN=example.net

# Hostname exposed by Traefik on the local network and Tailscale.
JELLYFIN_HOST=jellyfin.${DOMAIN}

# Numeric GID of the host's render group (`getent group render`).
JELLYFIN_RENDER_GROUP_ID=105

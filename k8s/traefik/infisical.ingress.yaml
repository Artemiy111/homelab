apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: infisical
  namespace: default
spec:
  ingressClassName: traefik
  rules:
    - host: infisical.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: infisical
                port:
                  number: 8080

apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: netdata
  namespace: default
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`netdata.example.com`)
      kind: Rule
      middlewares:
        - name: oauth2-proxy
        - name: secure-headers
        - name: ratelimit-default
      services:
        - name: netdata
          port: 80
  tls:
    secretName: wildcard-tls

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stirlingpdf
  namespace: default
spec:
  ingressClassName: traefik
  rules:
    - host: pdf.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: stirlingpdf
                port:
                  number: 8080
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sure
  namespace: default
spec:
  ingressClassName: traefik
  rules:
    - host: sure.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sure
                port:
                  number: 3000

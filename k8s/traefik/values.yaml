# Traefik в k8s получает внешний IP от MetalLB (LoadBalancer) —
# слушает 80/443 на выделенном IP 192.0.2.10, не конфликтуя с Docker-Traefik (80/443 на .254).
# Схема чарта traefik-41.4.0 (strict JSON schema).

service:
  spec:
    type: LoadBalancer

ports:
  web:
    port: 8000
    expose:
      default: true
    protocol: TCP
  websecure:
    port: 8443
    expose:
      default: true
    protocol: TCP

ingressClass:
  enabled: true
  isDefaultClass: true
  name: traefik

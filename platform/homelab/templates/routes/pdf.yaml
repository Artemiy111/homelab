apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stirlingpdf
  namespace: pdf
spec:
  ingressClassName: traefik
  rules:
    - host: pdf.{{ .Values.config.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: stirlingpdf
                port:
                  number: 8080